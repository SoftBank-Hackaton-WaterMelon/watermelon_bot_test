name: Lambda ChatOps Test

on:
  repository_dispatch:
    types: [dev_deploy]
  workflow_dispatch:

jobs:
  test:
    name: Test Lambda ChatOps
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Lambda ChatOps Test Trigger
        run: |
          echo "=========================================="
          echo "✅ Lambda ChatOps 트리거 테스트 성공!"
          echo "=========================================="
          echo "📦 이 워크플로우는 테스트용입니다."
          echo ""
          echo "Payload 정보:"
          echo "  - Message: ${{ github.event.client_payload.message || 'N/A' }}"
          echo "  - User: ${{ github.event.client_payload.user || 'N/A' }}"
          echo "  - Timestamp: ${{ github.event.client_payload.timestamp || 'N/A' }}"
          echo ""
          echo "⚠️ 참고:"
          echo "  이 Repository는 Lambda 코드만 관리합니다."
          echo "  실제 배포는 demo-backend, demo-repository에서 수행됩니다."
          echo "=========================================="
          echo "✅ 테스트 완료"
          echo "=========================================="
